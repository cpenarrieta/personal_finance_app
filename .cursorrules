# Cursor Rules for Personal Finance App

## Primary Documentation

Read `CLAUDE.md` for critical development rules and quick reference.

Read detailed documentation in `docs/` folder:
- `docs/DATA_FETCHING.md` - Server Components, props pattern, serialization
- `docs/ARCHITECTURE.md` - Database schema, Plaid sync, project structure
- `docs/DEVELOPMENT.md` - Commands, environment setup, testing
- `docs/MIGRATIONS.md` - Database migrations process and best practices

## Critical Rules Summary

### 1. Data Fetching ⭐️ MOST IMPORTANT
- ALWAYS fetch categories/tags in Server Components
- NEVER fetch categories/tags in client components with useEffect
- Pass reference data as props through component tree
- Components requiring props: SearchableTransactionList, EditTransactionModal, SplitTransactionModal

### 2. UI Components ⭐️ ALWAYS USE shadcn/ui
- NEVER create custom form inputs, selects, badges, or alerts
- USE shadcn/ui components from `src/components/ui/`
- Available: Input, Select, Label, Textarea, Button, Badge, Alert, Card, Dialog, Checkbox, Switch, Tabs, Table, Popover, Separator, RadioGroup, ScrollArea
- Exception: Native `<select>` with `<optgroup>` is OK (shadcn doesn't support optgroups)
- Always pair Label with form inputs

### 3. Component Patterns
- Server Components by default
- Use 'use client' only when needed
- TypeScript strict mode enabled
- Use @/* path aliases

## Tech Stack
- Next.js 15 (App Router) + TypeScript
- PostgreSQL + Prisma ORM
- Better Auth (OAuth)
- Plaid API
- shadcn/ui + Tailwind CSS 4

## Commands
- Never run: `npm run dev`
- Database: `npx prisma migrate dev`, `npx prisma generate`, `npx prisma studio`
- Sync: `npm run sync`

For complete details, always refer to CLAUDE.md and docs/ folder.
