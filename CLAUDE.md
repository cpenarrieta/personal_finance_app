# CLAUDE.md

## Rules

1. **Colors**: shadcn theme variables only. No hardcoded Tailwind colors.
2. **Components**: shadcn/ui for all UI. Exception: native `<select>` with `<optgroup>`.
3. **Dev server**: Never run `npm run dev`.
